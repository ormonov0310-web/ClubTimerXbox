{
  "rules": {
    ".read": "auth != null && root.child('security/allowedUids').child(auth.uid).val() === true",
    ".write": "auth != null && root.child('security/allowedUids').child(auth.uid).val() === true",
    "clubs": {
      "$clubId": {
        "commands": {
          ".indexOn": ["status"]
        }
      }
    }
  }
}
